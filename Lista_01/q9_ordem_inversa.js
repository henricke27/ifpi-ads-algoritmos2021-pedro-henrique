    var input = require('prompt-sync')()
    
    //Entrada

    var numero_1 = Number(input('Digite o primeiro número: '))
    var numero_2 = Number(input('Digite o segundo número: '))

    //Processamento e saída

    console.log(`Você digitou ${numero_2} e ${numero_1}.`)

